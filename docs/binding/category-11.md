---
title: 바인딩 목록 - 이름/식별자
outline: false
---

# 이름/식별자 {#binding-category}

총 87개 바인딩

[카테고리 목록으로 돌아가기](./index.html#binding-index)

<details>
<summary><strong>#active_skin_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
active skin의 이름

**자세한 설명**
이 바인딩은 'active skin'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: active skin 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "active skin_name_label": {
    "type": "label",
    "text": "#active skin_name",
    "bindings": [
      {
        "binding_name": "#active skin_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona SDL (총 1개 파일)

</details>

<details>
<summary><strong>#active_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
active world의 이름

**자세한 설명**
이 바인딩은 'active world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: active world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "active world_name_label": {
    "type": "label",
    "text": "#active world_name",
    "bindings": [
      {
        "binding_name": "#active world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms settings screen (총 1개 파일)

</details>

<details>
<summary><strong>#ad_account_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
ad account의 이름

**자세한 설명**
이 바인딩은 'ad account'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: ad account 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "ad account_name_label": {
    "type": "label",
    "text": "#ad account_name",
    "bindings": [
      {
        "binding_name": "#ad account_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/general section (총 1개 파일)

</details>

<details>
<summary><strong>#animation_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
animation의 이름

**자세한 설명**
이 바인딩은 'animation'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: animation 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "animation_name_label": {
    "type": "label",
    "text": "#animation_name",
    "bindings": [
      {
        "binding_name": "#animation_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
game tip screen (총 1개 파일)

</details>

<details>
<summary><strong>#audible_keymapping_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
audible keymapping의 이름

**자세한 설명**
이 바인딩은 'audible keymapping'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: audible keymapping 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "audible keymapping_name_label": {
    "type": "label",
    "text": "#audible keymapping_name",
    "bindings": [
      {
        "binding_name": "#audible keymapping_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/controls section (총 1개 파일)

</details>

<details>
<summary><strong>#banner_hover_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
banner hover texture의 이름

**자세한 설명**
이 바인딩은 'banner hover texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: banner hover texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "banner hover texture_name_label": {
    "type": "label",
    "text": "#banner hover texture_name",
    "bindings": [
      {
        "binding_name": "#banner hover texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
csb sections/csb banner (총 1개 파일)

</details>

<details>
<summary><strong>#banner_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
banner texture의 이름

**자세한 설명**
이 바인딩은 'banner texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: banner texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "banner texture_name_label": {
    "type": "label",
    "text": "#banner texture_name",
    "bindings": [
      {
        "binding_name": "#banner texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
csb sections/csb banner, store promo timeline screen (총 2개 파일)

</details>

<details>
<summary><strong>#beta_retail_legacy_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
beta retail legacy world의 이름

**자세한 설명**
이 바인딩은 'beta retail legacy world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: beta retail legacy world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "beta retail legacy world_name_label": {
    "type": "label",
    "text": "#beta retail legacy world_name",
    "bindings": [
      {
        "binding_name": "#beta retail legacy world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#beta_retail_local_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
beta retail local world의 이름

**자세한 설명**
이 바인딩은 'beta retail local world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: beta retail local world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "beta retail local world_name_label": {
    "type": "label",
    "text": "#beta retail local world_name",
    "bindings": [
      {
        "binding_name": "#beta retail local world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#branch_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
branch의 이름

**자세한 설명**
이 바인딩은 'branch'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: branch 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "branch_name_label": {
    "type": "label",
    "text": "#branch_name",
    "bindings": [
      {
        "binding_name": "#branch_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms settings screen (총 1개 파일)

</details>

<details>
<summary><strong>#bundle_coin_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
bundle coin texture의 이름

**자세한 설명**
이 바인딩은 'bundle coin texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: bundle coin texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "bundle coin texture_name_label": {
    "type": "label",
    "text": "#bundle coin texture_name",
    "bindings": [
      {
        "binding_name": "#bundle coin texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
coin purchase screen (총 1개 파일)

</details>

<details>
<summary><strong>#bundle_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
bundle의 이름

**자세한 설명**
이 바인딩은 'bundle'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: bundle 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "bundle_name_label": {
    "type": "label",
    "text": "#bundle_name",
    "bindings": [
      {
        "binding_name": "#bundle_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
gameplay common (총 1개 파일)

</details>

<details>
<summary><strong>#bundle_thumbnail_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
bundle thumbnail texture의 이름

**자세한 설명**
이 바인딩은 'bundle thumbnail texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: bundle thumbnail texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "bundle thumbnail texture_name_label": {
    "type": "label",
    "text": "#bundle thumbnail texture_name",
    "bindings": [
      {
        "binding_name": "#bundle thumbnail texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
coin purchase screen (총 1개 파일)

</details>

<details>
<summary><strong>#can_change_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
can change의 이름

**자세한 설명**
이 바인딩은 'can change'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: can change 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "can change_name_label": {
    "type": "label",
    "text": "#can change_name",
    "bindings": [
      {
        "binding_name": "#can change_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/general section (총 1개 파일)

</details>

<details>
<summary><strong>#close_button_visible_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
close button visible binding의 이름

**자세한 설명**
이 바인딩은 'close button visible binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: close button visible binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "close button visible binding_name_label": {
    "type": "label",
    "text": "#close button visible binding_name",
    "bindings": [
      {
        "binding_name": "#close button visible binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
command block screen (총 1개 파일)

</details>

<details>
<summary><strong>#cloud_file_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
cloud file의 이름

**자세한 설명**
이 바인딩은 'cloud file'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: cloud file 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "cloud file_name_label": {
    "type": "label",
    "text": "#cloud file_name",
    "bindings": [
      {
        "binding_name": "#cloud file_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/world section (총 1개 파일)

</details>

<details>
<summary><strong>#coin_offer_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
coin offer texture의 이름

**자세한 설명**
이 바인딩은 'coin offer texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: coin offer texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "coin offer texture_name_label": {
    "type": "label",
    "text": "#coin offer texture_name",
    "bindings": [
      {
        "binding_name": "#coin offer texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
coin purchase screen (총 1개 파일)

</details>

<details>
<summary><strong>#converted_world_preview_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
converted world preview의 이름

**자세한 설명**
이 바인딩은 'converted world preview'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: converted world preview 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "converted world preview_name_label": {
    "type": "label",
    "text": "#converted world preview_name",
    "bindings": [
      {
        "binding_name": "#converted world preview_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
world conversion complete screen (총 1개 파일)

</details>

<details>
<summary><strong>#creator_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
creator의 이름

**자세한 설명**
이 바인딩은 'creator'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: creator 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "creator_name_label": {
    "type": "label",
    "text": "#creator_name",
    "bindings": [
      {
        "binding_name": "#creator_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
expanded skin pack screen (총 1개 파일)

</details>

<details>
<summary><strong>#cross_platform_friends_server_icon_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
cross platform friends server icon texture의 이름

**자세한 설명**
이 바인딩은 'cross platform friends server icon texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: cross platform friends server icon texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "cross platform friends server icon texture_name_label": {
    "type": "label",
    "text": "#cross platform friends server icon texture_name",
    "bindings": [
      {
        "binding_name": "#cross platform friends server icon texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#current_skin_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
current skin의 이름

**자세한 설명**
이 바인딩은 'current skin'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: current skin 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "current skin_name_label": {
    "type": "label",
    "text": "#current skin_name",
    "bindings": [
      {
        "binding_name": "#current skin_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona SDL (총 1개 파일)

</details>

<details>
<summary><strong>#dev_discovery_override_service_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
dev discovery override service의 이름

**자세한 설명**
이 바인딩은 'dev discovery override service'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: dev discovery override service 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "dev discovery override service_name_label": {
    "type": "label",
    "text": "#dev discovery override service_name",
    "bindings": [
      {
        "binding_name": "#dev discovery override service_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/general section (총 1개 파일)

</details>

<details>
<summary><strong>#dynamic_tooltip_notification_visibility_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
dynamic tooltip notification visibility binding의 이름

**자세한 설명**
이 바인딩은 'dynamic tooltip notification visibility binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: dynamic tooltip notification visibility binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "dynamic tooltip notification visibility binding_name_label": {
    "type": "label",
    "text": "#dynamic tooltip notification visibility binding_name",
    "bindings": [
      {
        "binding_name": "#dynamic tooltip notification visibility binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui template buttons (총 1개 파일)

</details>

<details>
<summary><strong>#edu_cloud_conflict_resolution_cloud_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
edu cloud conflict resolution cloud의 이름

**자세한 설명**
이 바인딩은 'edu cloud conflict resolution cloud'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: edu cloud conflict resolution cloud 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "edu cloud conflict resolution cloud_name_label": {
    "type": "label",
    "text": "#edu cloud conflict resolution cloud_name",
    "bindings": [
      {
        "binding_name": "#edu cloud conflict resolution cloud_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui edu common (총 1개 파일)

</details>

<details>
<summary><strong>#edu_cloud_conflict_resolution_local_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
edu cloud conflict resolution local의 이름

**자세한 설명**
이 바인딩은 'edu cloud conflict resolution local'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: edu cloud conflict resolution local 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "edu cloud conflict resolution local_name_label": {
    "type": "label",
    "text": "#edu cloud conflict resolution local_name",
    "bindings": [
      {
        "binding_name": "#edu cloud conflict resolution local_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui edu common (총 1개 파일)

</details>

<details>
<summary><strong>#emote_index_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
emote index의 이름

**자세한 설명**
이 바인딩은 'emote index'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: emote index 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "emote index_name_label": {
    "type": "label",
    "text": "#emote index_name",
    "bindings": [
      {
        "binding_name": "#emote index_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona common (총 1개 파일)

</details>

<details>
<summary><strong>#emote_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
emote의 이름

**자세한 설명**
이 바인딩은 'emote'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: emote 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "emote_name_label": {
    "type": "label",
    "text": "#emote_name",
    "bindings": [
      {
        "binding_name": "#emote_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona common (총 1개 파일)

</details>

<details>
<summary><strong>#friends_server_icon_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
friends server icon texture의 이름

**자세한 설명**
이 바인딩은 'friends server icon texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: friends server icon texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "friends server icon texture_name_label": {
    "type": "label",
    "text": "#friends server icon texture_name",
    "bindings": [
      {
        "binding_name": "#friends server icon texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#gathering_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
gathering의 이름

**자세한 설명**
이 바인딩은 'gathering'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: gathering 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "gathering_name_label": {
    "type": "label",
    "text": "#gathering_name",
    "bindings": [
      {
        "binding_name": "#gathering_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/general section (총 1개 파일)

</details>

<details>
<summary><strong>#group_item_group_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
group item group의 이름

**자세한 설명**
이 바인딩은 'group item group'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: group item group 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "group item group_name_label": {
    "type": "label",
    "text": "#group item group_name",
    "bindings": [
      {
        "binding_name": "#group item group_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui common (총 1개 파일)

</details>

<details>
<summary><strong>#icon_overlay_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
icon overlay texture의 이름

**자세한 설명**
이 바인딩은 'icon overlay texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: icon overlay texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "icon overlay texture_name_label": {
    "type": "label",
    "text": "#icon overlay texture_name",
    "bindings": [
      {
        "binding_name": "#icon overlay texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
store common (총 1개 파일)

</details>

<details>
<summary><strong>#include_entities_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
include entities binding의 이름

**자세한 설명**
이 바인딩은 'include entities binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: include entities binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "include entities binding_name_label": {
    "type": "label",
    "text": "#include entities binding_name",
    "bindings": [
      {
        "binding_name": "#include entities binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#include_players_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
include players binding의 이름

**자세한 설명**
이 바인딩은 'include players binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: include players binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "include players binding_name_label": {
    "type": "label",
    "text": "#include players binding_name",
    "bindings": [
      {
        "binding_name": "#include players binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#info_additional_server_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
info additional server의 이름

**자세한 설명**
이 바인딩은 'info additional server'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: info additional server 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "info additional server_name_label": {
    "type": "label",
    "text": "#info additional server_name",
    "bindings": [
      {
        "binding_name": "#info additional server_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#info_additional_server_ping_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
info additional server ping texture의 이름

**자세한 설명**
이 바인딩은 'info additional server ping texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: info additional server ping texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "info additional server ping texture_name_label": {
    "type": "label",
    "text": "#info additional server ping texture_name",
    "bindings": [
      {
        "binding_name": "#info additional server ping texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#info_ping_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
info ping texture의 이름

**자세한 설명**
이 바인딩은 'info ping texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: info ping texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "info ping texture_name_label": {
    "type": "label",
    "text": "#info ping texture_name",
    "bindings": [
      {
        "binding_name": "#info ping texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#info_third_party_server_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
info third party server의 이름

**자세한 설명**
이 바인딩은 'info third party server'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: info third party server 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "info third party server_name_label": {
    "type": "label",
    "text": "#info third party server_name",
    "bindings": [
      {
        "binding_name": "#info third party server_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#keymapping_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
keymapping의 이름

**자세한 설명**
이 바인딩은 'keymapping'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: keymapping 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "keymapping_name_label": {
    "type": "label",
    "text": "#keymapping_name",
    "bindings": [
      {
        "binding_name": "#keymapping_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/controls section (총 1개 파일)

</details>

<details>
<summary><strong>#legacy_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
legacy world의 이름

**자세한 설명**
이 바인딩은 'legacy world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: legacy world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "legacy world_name_label": {
    "type": "label",
    "text": "#legacy world_name",
    "bindings": [
      {
        "binding_name": "#legacy world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#link_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
link의 이름

**자세한 설명**
이 바인딩은 'link'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: link 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "link_name_label": {
    "type": "label",
    "text": "#link_name",
    "bindings": [
      {
        "binding_name": "#link_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms settings screen (총 1개 파일)

</details>

<details>
<summary><strong>#local_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
local world의 이름

**자세한 설명**
이 바인딩은 'local world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: local world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "local world_name_label": {
    "type": "label",
    "text": "#local world_name",
    "bindings": [
      {
        "binding_name": "#local world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#matching_branch_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
matching branch의 이름

**자세한 설명**
이 바인딩은 'matching branch'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: matching branch 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "matching branch_name_label": {
    "type": "label",
    "text": "#matching branch_name",
    "bindings": [
      {
        "binding_name": "#matching branch_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms settings screen (총 1개 파일)

</details>

<details>
<summary><strong>#minimize_button_visible_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
minimize button visible binding의 이름

**자세한 설명**
이 바인딩은 'minimize button visible binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: minimize button visible binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "minimize button visible binding_name_label": {
    "type": "label",
    "text": "#minimize button visible binding_name",
    "bindings": [
      {
        "binding_name": "#minimize button visible binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
command block screen (총 1개 파일)

</details>

<details>
<summary><strong>#mob_effect_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
mob effect의 이름

**자세한 설명**
이 바인딩은 'mob effect'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: mob effect 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "mob effect_name_label": {
    "type": "label",
    "text": "#mob effect_name",
    "bindings": [
      {
        "binding_name": "#mob effect_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
mob effect screen (총 1개 파일)

</details>

<details>
<summary><strong>#objective_sidebar_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
objective sidebar의 이름

**자세한 설명**
이 바인딩은 'objective sidebar'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: objective sidebar 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "objective sidebar_name_label": {
    "type": "label",
    "text": "#objective sidebar_name",
    "bindings": [
      {
        "binding_name": "#objective sidebar_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
scoreboards (총 1개 파일)

</details>

<details>
<summary><strong>#offer_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
offer texture의 이름

**자세한 설명**
이 바인딩은 'offer texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: offer texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "offer texture_name_label": {
    "type": "label",
    "text": "#offer texture_name",
    "bindings": [
      {
        "binding_name": "#offer texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui purchase common, world templates screen (총 2개 파일)

</details>

<details>
<summary><strong>#option_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
option의 이름

**자세한 설명**
이 바인딩은 'option'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: option 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "option_name_label": {
    "type": "label",
    "text": "#option_name",
    "bindings": [
      {
        "binding_name": "#option_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
permissions screen (총 1개 파일)

</details>

<details>
<summary><strong>#option_state_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
option state의 이름

**자세한 설명**
이 바인딩은 'option state'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: option state 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "option state_name_label": {
    "type": "label",
    "text": "#option state_name",
    "bindings": [
      {
        "binding_name": "#option state_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
permissions screen (총 1개 파일)

</details>

<details>
<summary><strong>#output_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
output의 이름

**자세한 설명**
이 바인딩은 'output'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: output 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "output_name_label": {
    "type": "label",
    "text": "#output_name",
    "bindings": [
      {
        "binding_name": "#output_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
furnace screen pocket (총 1개 파일)

</details>

<details>
<summary><strong>#pack_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
pack의 이름

**자세한 설명**
이 바인딩은 'pack'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: pack 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "pack_name_label": {
    "type": "label",
    "text": "#pack_name",
    "bindings": [
      {
        "binding_name": "#pack_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
expanded skin pack screen, skin picker screen (총 2개 파일)

</details>

<details>
<summary><strong>#placeholder_realm_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
placeholder realm의 이름

**자세한 설명**
이 바인딩은 'placeholder realm'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: placeholder realm 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "placeholder realm_name_label": {
    "type": "label",
    "text": "#placeholder realm_name",
    "bindings": [
      {
        "binding_name": "#placeholder realm_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realmsPlus sections/realmsPlus buy now screen, realms create (총 2개 파일)

</details>

<details>
<summary><strong>#preview_skin_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
preview skin의 이름

**자세한 설명**
이 바인딩은 'preview skin'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: preview skin 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "preview skin_name_label": {
    "type": "label",
    "text": "#preview skin_name",
    "bindings": [
      {
        "binding_name": "#preview skin_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona SDL (총 1개 파일)

</details>

<details>
<summary><strong>#real_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
real의 이름

**자세한 설명**
이 바인딩은 'real'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: real 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "real_name_label": {
    "type": "label",
    "text": "#real_name",
    "bindings": [
      {
        "binding_name": "#real_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms allowlist (총 1개 파일)

</details>

<details>
<summary><strong>#realm_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
realm의 이름

**자세한 설명**
이 바인딩은 'realm'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: realm 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "realm_name_label": {
    "type": "label",
    "text": "#realm_name",
    "bindings": [
      {
        "binding_name": "#realm_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms pending invitations, settings sections/general section, settings sections/realms world section (총 3개 파일)

</details>

<details>
<summary><strong>#realms_plus_skin_pack_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
realms plus skin pack의 이름

**자세한 설명**
이 바인딩은 'realms plus skin pack'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: realms plus skin pack 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "realms plus skin pack_name_label": {
    "type": "label",
    "text": "#realms plus skin pack_name",
    "bindings": [
      {
        "binding_name": "#realms plus skin pack_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona popups (총 1개 파일)

</details>

<details>
<summary><strong>#remove_blocks_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
remove blocks binding의 이름

**자세한 설명**
이 바인딩은 'remove blocks binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: remove blocks binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "remove blocks binding_name_label": {
    "type": "label",
    "text": "#remove blocks binding_name",
    "bindings": [
      {
        "binding_name": "#remove blocks binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#resource_pack_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
resource pack의 이름

**자세한 설명**
이 바인딩은 'resource pack'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: resource pack 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "resource pack_name_label": {
    "type": "label",
    "text": "#resource pack_name",
    "bindings": [
      {
        "binding_name": "#resource pack_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
progress screen (총 1개 파일)

</details>

<details>
<summary><strong>#save_world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
save world의 이름

**자세한 설명**
이 바인딩은 'save world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: save world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "save world_name_label": {
    "type": "label",
    "text": "#save world_name",
    "bindings": [
      {
        "binding_name": "#save world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms settings screen (총 1개 파일)

</details>

<details>
<summary><strong>#screenshot_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
screenshot texture의 이름

**자세한 설명**
이 바인딩은 'screenshot texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: screenshot texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "screenshot texture_name_label": {
    "type": "label",
    "text": "#screenshot texture_name",
    "bindings": [
      {
        "binding_name": "#screenshot texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui purchase common (총 1개 파일)

</details>

<details>
<summary><strong>#search_box_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
search box의 이름

**자세한 설명**
이 바인딩은 'search box'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: search box 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "search box_name_label": {
    "type": "label",
    "text": "#search box_name",
    "bindings": [
      {
        "binding_name": "#search box_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ugc viewer screen (총 1개 파일)

</details>

<details>
<summary><strong>#selected_character_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
selected character의 이름

**자세한 설명**
이 바인딩은 'selected character'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: selected character 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "selected character_name_label": {
    "type": "label",
    "text": "#selected character_name",
    "bindings": [
      {
        "binding_name": "#selected character_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona cast character screen (총 1개 파일)

</details>

<details>
<summary><strong>#selected_control_scheme_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
selected control scheme의 이름

**자세한 설명**
이 바인딩은 'selected control scheme'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: selected control scheme 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "selected control scheme_name_label": {
    "type": "label",
    "text": "#selected control scheme_name",
    "bindings": [
      {
        "binding_name": "#selected control scheme_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/controls section (총 1개 파일)

</details>

<details>
<summary><strong>#selected_skin_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
selected skin의 이름

**자세한 설명**
이 바인딩은 'selected skin'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: selected skin 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "selected skin_name_label": {
    "type": "label",
    "text": "#selected skin_name",
    "bindings": [
      {
        "binding_name": "#selected skin_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
persona SDL (총 1개 파일)

</details>

<details>
<summary><strong>#show_bounding_box_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
show bounding box binding의 이름

**자세한 설명**
이 바인딩은 'show bounding box binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: show bounding box binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "show bounding box binding_name_label": {
    "type": "label",
    "text": "#show bounding box binding_name",
    "bindings": [
      {
        "binding_name": "#show bounding box binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#skin_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
skin의 이름

**자세한 설명**
이 바인딩은 'skin'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: skin 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "skin_name_label": {
    "type": "label",
    "text": "#skin_name",
    "bindings": [
      {
        "binding_name": "#skin_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
expanded skin pack screen, skin picker screen (총 2개 파일)

</details>

<details>
<summary><strong>#slot_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
slot의 이름

**자세한 설명**
이 바인딩은 'slot'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: slot 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "slot_name_label": {
    "type": "label",
    "text": "#slot_name",
    "bindings": [
      {
        "binding_name": "#slot_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
choose realm screen (총 1개 파일)

</details>

<details>
<summary><strong>#store_row_image_animation_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
store row image animation의 이름

**자세한 설명**
이 바인딩은 'store row image animation'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: store row image animation 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "store row image animation_name_label": {
    "type": "label",
    "text": "#store row image animation_name",
    "bindings": [
      {
        "binding_name": "#store row image animation_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
store promo timeline screen (총 1개 파일)

</details>

<details>
<summary><strong>#store_row_image_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
store row image texture의 이름

**자세한 설명**
이 바인딩은 'store row image texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: store row image texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "store row image texture_name_label": {
    "type": "label",
    "text": "#store row image texture_name",
    "bindings": [
      {
        "binding_name": "#store row image texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
store promo timeline screen (총 1개 파일)

</details>

<details>
<summary><strong>#structure_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
structure의 이름

**자세한 설명**
이 바인딩은 'structure'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: structure 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "structure_name_label": {
    "type": "label",
    "text": "#structure_name",
    "bindings": [
      {
        "binding_name": "#structure_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#structure_name_content_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
structure content binding의 이름

**자세한 설명**
이 바인딩은 'structure content binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: structure content binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "structure content binding_name_label": {
    "type": "label",
    "text": "#structure content binding_name",
    "bindings": [
      {
        "binding_name": "#structure content binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#tele_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
tele의 이름

**자세한 설명**
이 바인딩은 'tele'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: tele 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "tele_name_label": {
    "type": "label",
    "text": "#tele_name",
    "bindings": [
      {
        "binding_name": "#tele_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
host options screen (총 1개 파일)

</details>

<details>
<summary><strong>#template_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
template의 이름

**자세한 설명**
이 바인딩은 'template'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: template 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "template_name_label": {
    "type": "label",
    "text": "#template_name",
    "bindings": [
      {
        "binding_name": "#template_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
custom templates screen (총 1개 파일)

</details>

<details>
<summary><strong>#text_box_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
text box의 이름

**자세한 설명**
이 바인딩은 'text box'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: text box 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "text box_name_label": {
    "type": "label",
    "text": "#text box_name",
    "bindings": [
      {
        "binding_name": "#text box_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui common (총 1개 파일)

</details>

<details>
<summary><strong>#text_box_placeholder_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
text box placeholder의 이름

**자세한 설명**
이 바인딩은 'text box placeholder'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: text box placeholder 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "text box placeholder_name_label": {
    "type": "label",
    "text": "#text box placeholder_name",
    "bindings": [
      {
        "binding_name": "#text box placeholder_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
cartography screen (총 1개 파일)

</details>

<details>
<summary><strong>#texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
texture의 이름

**자세한 설명**
이 바인딩은 'texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "texture_name_label": {
    "type": "label",
    "text": "#texture_name",
    "bindings": [
      {
        "binding_name": "#texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen, ugc viewer screen (총 2개 파일)

</details>

<details>
<summary><strong>#third_party_profile_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
third party profile의 이름

**자세한 설명**
이 바인딩은 'third party profile'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: third party profile 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "third party profile_name_label": {
    "type": "label",
    "text": "#third party profile_name",
    "bindings": [
      {
        "binding_name": "#third party profile_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms allowlist (총 1개 파일)

</details>

<details>
<summary><strong>#third_party_server_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
third party server의 이름

**자세한 설명**
이 바인딩은 'third party server'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: third party server 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "third party server_name_label": {
    "type": "label",
    "text": "#third party server_name",
    "bindings": [
      {
        "binding_name": "#third party server_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
play screen (총 1개 파일)

</details>

<details>
<summary><strong>#tier_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
tier의 이름

**자세한 설명**
이 바인딩은 'tier'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: tier 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "tier_name_label": {
    "type": "label",
    "text": "#tier_name",
    "bindings": [
      {
        "binding_name": "#tier_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
trade 2 screen (총 1개 파일)

</details>

<details>
<summary><strong>#trending_row_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
trending row의 이름

**자세한 설명**
이 바인딩은 'trending row'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: trending row 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "trending row_name_label": {
    "type": "label",
    "text": "#trending row_name",
    "bindings": [
      {
        "binding_name": "#trending row_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
store search screen (총 1개 파일)

</details>

<details>
<summary><strong>#tts_offer_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
tts offer의 이름

**자세한 설명**
이 바인딩은 'tts offer'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: tts offer 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "tts offer_name_label": {
    "type": "label",
    "text": "#tts offer_name",
    "bindings": [
      {
        "binding_name": "#tts offer_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
patch notes screen (총 1개 파일)

</details>

<details>
<summary><strong>#waterlog_blocks_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
waterlog blocks binding의 이름

**자세한 설명**
이 바인딩은 'waterlog blocks binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: waterlog blocks binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "waterlog blocks binding_name_label": {
    "type": "label",
    "text": "#waterlog blocks binding_name",
    "bindings": [
      {
        "binding_name": "#waterlog blocks binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
structure editor screen (총 1개 파일)

</details>

<details>
<summary><strong>#world_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
world의 이름

**자세한 설명**
이 바인딩은 'world'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: world 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "world_name_label": {
    "type": "label",
    "text": "#world_name",
    "bindings": [
      {
        "binding_name": "#world_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/realms world section, settings sections/world section (총 2개 파일)

</details>

<details>
<summary><strong>#world_slot_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
world slot의 이름

**자세한 설명**
이 바인딩은 'world slot'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: world slot 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "world slot_name_label": {
    "type": "label",
    "text": "#world slot_name",
    "bindings": [
      {
        "binding_name": "#world slot_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
realms slots screen (총 1개 파일)

</details>

<details>
<summary><strong>#world_template_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
world template의 이름

**자세한 설명**
이 바인딩은 'world template'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: world template 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "world template_name_label": {
    "type": "label",
    "text": "#world template_name",
    "bindings": [
      {
        "binding_name": "#world template_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
world templates screen (총 1개 파일)

</details>

<details>
<summary><strong>#world_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
world texture의 이름

**자세한 설명**
이 바인딩은 'world texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: world texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "world texture_name_label": {
    "type": "label",
    "text": "#world texture_name",
    "bindings": [
      {
        "binding_name": "#world texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
settings sections/world section (총 1개 파일)

</details>

<details>
<summary><strong>#Xout_button_visible_binding_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
xout button visible binding의 이름

**자세한 설명**
이 바인딩은 'xout button visible binding'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: xout button visible binding 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "xout button visible binding_name_label": {
    "type": "label",
    "text": "#xout button visible binding_name",
    "bindings": [
      {
        "binding_name": "#xout button visible binding_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
command block screen (총 1개 파일)

</details>

<details>
<summary><strong>#zoomed_texture_name</strong> - string (문자열)</summary>

**데이터 타입:** string (문자열)

**간단 설명**
zoomed texture의 이름

**자세한 설명**
이 바인딩은 'zoomed texture'의 이름이나 명칭을 제공합니다.

주로 식별이나 표시 목적으로 사용되며,
사용자가 읽을 수 있는 친숙한 이름입니다.

**실제 사용 예**
예: zoomed texture 이름 표시, 목록에서 항목 식별

**코드 예제**
```jsonc
{
  "zoomed texture_name_label": {
    "type": "label",
    "text": "#zoomed texture_name",
    "bindings": [
      {
        "binding_name": "#zoomed texture_name",
        "binding_name_override": "#text",
        "binding_type": "global"
      }
    ]
  }
}
```

**사용 화면**
ui purchase common (총 1개 파일)


====================================================================================================
바인딩 사용 팁
====================================================================================================

1. 바인딩 이름은 항상 # 으로 시작합니다
2. binding_type은 보통 "global"을 사용합니다
3. binding_name_override를 사용하면 다른 속성에 연결할 수 있습니다
4. view 바인딩으로 Molang 표현식을 사용해 조건부 로직을 만들 수 있습니다
5. collection 바인딩으로 동적 리스트를 만들 수 있습니다

더 배우고 싶다면:
- Bedrock Wiki: https://wiki.bedrock.dev/
- JSON UI 문서: https://bedrock.dev/docs/stable/JSON%20UI
- Molang 문서: https://bedrock.dev/docs/stable/Molang

</details>

